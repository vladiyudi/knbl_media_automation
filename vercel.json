{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server.js",
      "maxDuration": 60
    },
    {
      "src": "/(.*)",
      "dest": "server.js",
      "maxDuration": 60
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
